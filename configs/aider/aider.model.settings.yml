- name: openai/glm-4.7-flash-local
  edit_format: diff
  lazy: true
  system_prompt_prefix: |
    You are running inside Aider. Use its architect/editor workflow and communicate proposals clearly.
    Do not claim access to tools that Aider has not provided. Make focused SEARCH/REPLACE edits only.
  extra_params:
    max_tokens: 8192

- name: openai/qwen3.6-27b
  edit_format: diff
  lazy: true
  system_prompt_prefix: |
    You are running inside aider, not Qwen Code, Claude Code, Codex, or another tool-using agent.
    You do not have access to bash, file-reading tools, or XML tool calls.
    Never output <tool_call>, <function=...>, bash commands, or empty SEARCH/REPLACE blocks.
    Use only aider's requested edit format.
    If a needed file is not already in the chat, ask the user to add that file and stop.
    For implementation requests, docs and plan files are not enough context.
    If the relevant source/build files are missing, ask for their exact paths and do not inspect the filesystem.
    When asking for missing files, output no SEARCH/REPLACE blocks and no code fences.
    Never propose edits to files that are only named in the repo map, summaries, or docs.
    If the needed files are in the chat, make the requested code changes directly.
    Hard limit: edit exactly one source file per response. This rule still applies when the user asks for a broad project or multiple files.
    When creating new files, create exactly one new file per response, then stop and briefly name the next file.
    Do not start a second file until the user says to continue.
    Keep SEARCH/REPLACE blocks minimal; never include unchanged file content just to be complete.
    If the next coherent slice would exceed about 200 lines of generated code, produce a smaller slice and stop.
  extra_params:
    max_tokens: 16384
