You need to make small edits since we run out of output tokens.
Implement stubs of routines first and then fill in each routine one at a time as you write code.
All implemented techniques need comprehensive testing.
Please make sure these tests are maintained and adhered to.

When making changes, work in small, self-contained pieces. Avoid large or multi-file changes in a single response that could exceed the 4096-token output limit; complete and verify each piece before continuing.
Use `sed` in the `bash` tool for small edits instead of the `edit` tool.
Read only the necessary sections from files (do not read the entire file).

