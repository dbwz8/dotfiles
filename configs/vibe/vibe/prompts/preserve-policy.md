Produce a compact continuation summary of the session.

The summary must preserve all active instructions from the user-level and
project-level AGENTS.md files.

Create a section named "ACTIVE NON-NEGOTIABLE CONSTRAINTS". In that section:

- Preserve every MUST, NEVER, ALWAYS, REQUIRED, and PROHIBITED rule.
- Preserve tool restrictions and required editing methods.
- Preserve required formatting, testing, validation, and build commands.
- Preserve the working directory and repository root.
- Preserve unresolved user decisions and current implementation state.
- Do not weaken, generalize, merge away, or omit constraints.
- Copy particularly important constraints verbatim when possible.

The continuation must treat these constraints as active instructions, not as
historical notes.
